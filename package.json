{
  "name": "review-bot",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "review": "ts-node src/review.ts",
    "start:prod": "node dist/review",
    "lint": "eslint --fix",
    "prettier": "prettier -w src/. --write",
    "pre-commit": "npm run prettier && npm run lint",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Aleksey Romanov",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@typescript-eslint/eslint-plugin": "^8.7.0",
    "@typescript-eslint/parser": "^8.7.0",
    "eslint": "^8.57.1",
    "husky": "^9.1.7",
    "lint-staged": "^16.1.5",
    "prettier": "^3.6.2",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.6.3"
  },
  "lint-staged": {
    "*.ts": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.{js,json,md,yml,cy.ts}": [
      "prettier --write"
    ]
  }
}
